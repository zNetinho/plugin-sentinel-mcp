{
  "name": "mcp-runrunit",
  "version": "1.0.0",
  "description": "MCP server for Runrun.it API - Tasks and Comments",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "bin": {
    "mcp-runrunit": "./dist/index.js"
  }
}
