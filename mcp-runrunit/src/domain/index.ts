/**
 * Domain layer: types and ports (interfaces) used by the application.
 * In a future evolution, the Runrun.it API can be defined here as a port
 * and implemented by the driven adapter (adapters/driven/api.ts).
 */
